import { onAuthChange, loginWithGoogle, logout } from '../../lib/firebase/auth';
import type { AuthUser } from '../../types';

export { onAuthChange, loginWithGoogle, logout };
export type { AuthUser };